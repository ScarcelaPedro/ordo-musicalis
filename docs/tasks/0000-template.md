---
status: backlog
modulo: geral
owner:
criado-em: AAAA-MM-DD
---

# 0000 — Título curto da task

**Task ID**: `TASK-0000` (número deste arquivo — não introduza UUID, Jira ID ou outro identificador externo)

## Objetivo

O que precisa ser feito e por quê. Se vier de uma SPEC (`docs/specs/`), referencie a(s) seção(ões) relevante(s) dela. Se vier diretamente do roadmap/scope do projeto (sem SPEC formal), referencie a seção relevante de [`docs/SCOPE.md`](../SCOPE.md) (ou o roadmap específico do projeto, se houver um).

## Dependências

- `TASK-NNNN` — o que precisa estar concluído antes desta task poder começar (ou "Nenhuma").

## Critérios de conclusão

- [ ] Critério verificável 1
- [ ] Critério verificável 2

## Referências

Aponte só para os documentos necessários a esta task especificamente — não copie conteúdo extenso do roadmap/architecture docs para cá.

- `docs/decisions/NNNN-*.md` (se já existir ADR relacionado)

## Notas de progresso

- AAAA-MM-DD — o que foi feito nesta sessão, decisões tomadas (linkar `docs/decisions/NNNN-*.md` se aplicável), commits relevantes (hash curto, ex. `Commit: \`abc1234\``), bloqueios encontrados, próximo passo.
